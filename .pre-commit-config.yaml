repos:
  - repo: local
    hooks:
      - id: git-size-guard
        name: Git size guard (100MB)
        entry: python3 scripts/check_git_size.py
        language: system
        stages: [pre-commit, pre-push]
