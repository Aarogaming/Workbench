# Android Environment Baseline

- Android Studio (latest stable), SDK 34+, Build Tools 34.x
- Java 17
- AGP 8.x (scaffold uses 8.2.2)
- Verify: `./gradlew tasks` (after adding wrapper)
- Build/test: `./gradlew assembleDebug`, `./gradlew test`, `./gradlew connectedAndroidTest`
