# .NET Environment Baseline

- SDK: .NET 8 (for AAS C#/Maelstrom projects)
- Restore/build: `dotnet restore`, `dotnet build -c Release`
- Test: `dotnet test -c Release`

Keep solutions/references stable (Maelstrom lives under AAS as a child repo).
