# UX Tokens (Template)
- Define semantic tokens (colors, spacing, radius, typography).
- Keep initial values identical to current visuals; change values only with approval.
- Use tokens in UI surfaces instead of hard-coded values.
- Framework: {{FRAMEWORK}}
