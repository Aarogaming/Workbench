# JS/Node Environment Baseline

- Node: LTS 20.x recommended
- Install deps: `npm install`
- Build: `npm run build` (per repo)
- Test: `npm test` or `npm run test` (per repo)

Keep per-repo package-locks; align on Node 20.x to avoid mismatched engines.